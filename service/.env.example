# SQLLineage FastAPI Service Configuration
# Copy this file to .env and update with your values

# ===== Service Configuration =====
SERVICE_NAME=sqllineage-api
SERVICE_VERSION=1.0.0
SERVICE_PORT=8000
SERVICE_HOST=0.0.0.0

# ===== Logging Configuration =====
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path (inside container)
LOG_FILE=/app/logs/sqllineage-api.log

# Log format: json or text
LOG_FORMAT=json

# ===== OpenMetadata Configuration =====
# OpenMetadata API base URL (e.g., https://openmetadata.example.com/api/v1)
# Leave empty to disable OpenMetadata integration
OPENMETADATA_URL=

# OpenMetadata API key for authentication
OPENMETADATA_API_KEY=

# Request timeout for OpenMetadata API calls (seconds)
OPENMETADATA_TIMEOUT=10

# Cache TTL for OpenMetadata results (seconds)
OPENMETADATA_CACHE_TTL=300

# ===== Request Limits =====
# Maximum SQL request size in MB
MAX_REQUEST_SIZE_MB=50

# Rate limit: requests per minute per IP
RATE_LIMIT_RPM=100

# Request processing timeout (seconds)
REQUEST_TIMEOUT_SEC=30

# ===== SQL Configuration =====
# Default SQL dialect (ansi, hive, sparksql, tsql, etc.)
DEFAULT_DIALECT=ansi
